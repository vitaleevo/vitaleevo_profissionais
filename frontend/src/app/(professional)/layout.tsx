import { AppShell } from "@/components/layout/app-shell";

export default function ProfessionalLayout({ children }: { children: React.ReactNode }) {
  return <AppShell verticalKey="profi_angola">{children}</AppShell>;
}
